<!DOCTYPE html>
<html>

<head>
    <style>
        body {
            font-family: sans-serif;
            font-size: 12px;
        }

        .header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
        }

        .title {
            font-size: 24px;
            font-weight: bold;
            color: #333;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }

        .box {
            border: 1px solid #ccc;
            padding: 10px;
        }

        .box-title {
            font-weight: bold;
            border-bottom: 1px solid #eee;
            margin-bottom: 5px;
            padding-bottom: 2px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th,
        td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            bg-color: #f8f8f8;
        }

        .totals {
            margin-top: 20px;
            float: right;
            width: 300px;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 5px 0;
        }

        .grand-total {
            font-weight: bold;
            font-size: 14px;
            border-top: 2px solid #333;
            margin-top: 5px;
        }
    </style>
</head>

<body>
    <div class="header">
        <div>
            <div class="title">PROFORMA INVOICE</div>
            <div>Date: {{date}}</div>
            <div>Reference: {{shipment.id}}</div>
        </div>
        <div>
            <!-- Logo placeholder -->
        </div>
    </div>

    <div class="info-grid">
        <div class="box">
            <div class="box-title">SHIPPER / EXPORTER</div>
            <div>{{shipment.shipper.name}}</div>
            <div>{{shipment.shipper.addressLine1}}</div>
            <div>{{shipment.shipper.city}}, {{shipment.shipper.stateOrProvince}} {{shipment.shipper.postalCode}}</div>
            <div>{{shipment.shipper.countryCode}}</div>
            <div>Tax ID: {{shipment.shipper.taxIdOrEori}}</div>
        </div>
        <div class="box">
            <div class="box-title">CONSIGNEE</div>
            <div>{{shipment.consignee.name}}</div>
            <div>{{shipment.consignee.addressLine1}}</div>
            <div>{{shipment.consignee.city}}, {{shipment.consignee.stateOrProvince}} {{shipment.consignee.postalCode}}
            </div>
            <div>{{shipment.consignee.countryCode}}</div>
        </div>
    </div>

    <div class="info-grid">
        <div class="box">
            <div class="box-title">SHIPMENT DETAILS</div>
            <div>Incoterm: {{shipment.incoterm}}</div>
            <div>Currency: {{shipment.currency}}</div>
            <div>Origin: {{shipment.originCountry}}</div>
            <div>Destination: {{shipment.destinationCountry}}</div>
        </div>
        <div class="box">
            <div class="box-title">TRANSPORT</div>
            <div>Mode: {{shipment.carrierCode}}</div>
            <div>Tracking: {{shipment.trackingNumber}}</div>
            <div>Total Weight: {{shipment.totalWeightKg}} kg</div>
            <div>Packages: {{shipment.numPackages}}</div>
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th>SKU</th>
                <th>Description</th>
                <th>HTS</th>
                <th>Origin</th>
                <th>Qty</th>
                <th>Unit Price</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {{#each shipment.lineItems}}
            <tr>
                <td>{{this.sku}}</td>
                <td>{{this.description}}</td>
                <td>{{this.htsCode}}</td>
                <td>{{this.countryOfOrigin}}</td>
                <td>{{this.quantity}} {{this.uom}}</td>
                <td>{{../shipment.currency}} {{this.unitValue}}</td>
                <td>{{../shipment.currency}} {{this.extendedValue}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <div class="totals">
        <div class="total-row">
            <span>Subtotal:</span>
            <span>{{shipment.currency}} {{shipment.totalCustomsValue}}</span>
        </div>
        <div class="total-row grand-total">
            <span>TOTAL VALUE:</span>
            <span>{{shipment.currency}} {{shipment.totalCustomsValue}}</span>
        </div>
    </div>

    <div style="clear: both; padding-top: 50px;">
        <p>I declare that all information contained in this invoice to be true and correct.</p>
        <p style="margin-top: 40px; border-top: 1px solid #000; width: 200px; padding-top: 5px;">Signature</p>
    </div>
</body>

</html>