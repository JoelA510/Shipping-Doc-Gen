<!DOCTYPE html>
<html>

<head>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 9px;
            padding: 20px;
            line-height: 1.2;
        }

        .form-title {
            text-align: center;
            font-size: 14px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .row {
            display: flex;
            margin-bottom: 3px;
        }

        .field {
            border: 1px solid #000;
            padding: 3px 5px;
            flex: 1;
            position: relative;
        }

        .field-label {
            font-size: 7px;
            font-weight: bold;
            display: block;
            margin-bottom: 2px;
        }

        .field-value {
            font-size: 9px;
            min-height: 12px;
        }

        .small-field {
            flex: 0.5;
        }

        .large-field {
            flex: 2;
        }

        .checkbox {
            display: inline-block;
            width: 10px;
            height: 10px;
            border: 1px solid #000;
            margin-right: 3px;
            vertical-align: middle;
        }

        .checkbox.checked::after {
            content: '✓';
            font-size: 10px;
            font-weight: bold;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 5px;
            font-size: 8px;
        }

        th,
        td {
            border: 1px solid #000;
            padding: 3px;
            text-align: left;
        }

        th {
            background: #f0f0f0;
            font-weight: bold;
            font-size: 7px;
        }

        .text-right {
            text-align: right;
        }

        .header-bar {
            background: #E8571D;
            color: white;
            padding: 5px;
            text-align: center;
            font-weight: bold;
            margin-bottom: 5px;
        }
    </style>
</head>

<body>
    <div class="header-bar">
        {{#if header.forwarder.name}}
        {{header.forwarder.name}}
        {{else}}
        {{header.forwarder}}
        {{/if}}
    </div>
    <div class="form-title">SHIPPER'S LETTER OF INSTRUCTIONS</div>

    <!-- Row 1: Date and Quote -->
    <div class="row">
        <div class="field small-field">
            <span class="field-label">Date:</span>
            <span class="field-value">{{date}}</span>
        </div>
        <div class="field small-field">
            <span class="field-label">Quote#</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 2: USPPI -->
    <div class="row">
        <div class="field large-field">
            <span class="field-label">1. U.S. PRINCIPLE PARTY IN INTEREST (Name and address including zip code)</span>
            <div class="field-value">
                {{#if header.shipper.name}}
                {{header.shipper.name}}<br>
                {{header.shipper.address}}{{#if header.shipper.address2}}, {{header.shipper.address2}}{{/if}}<br>
                {{header.shipper.city}}, {{header.shipper.state}} {{header.shipper.zip}}<br>
                {{header.shipper.country}}
                {{else}}
                {{header.shipper}}
                {{/if}}
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">2. USPPI EIN (IRS) NO. OR ID NO.</span>
            <span class="field-value"></span>
        </div>
        <div class="field small-field">
            <span class="field-label">3. PARTIES TO TRANSACTION</span>
            <div class="field-value">
                <span class="checkbox"></span> RELATED<br>
                <span class="checkbox checked"></span> NON-RELATED
            </div>
        </div>
    </div>

    <!-- Row 3: Intermediate Consignee -->
    <div class="row">
        <div class="field large-field">
            <span class="field-label">4. INTERMEDIATE CONSIGNEE (Complete name and address)</span>
            <div class="field-value">
                {{#if header.forwarder.name}}
                {{header.forwarder.name}}<br>
                {{header.forwarder.address}}{{#if header.forwarder.address2}}, {{header.forwarder.address2}}{{/if}}<br>
                {{header.forwarder.city}}, {{header.forwarder.state}} {{header.forwarder.zip}}<br>
                {{header.forwarder.country}}
                {{#if header.forwarder.fmcNumber}}<br>FMC NO. {{header.forwarder.fmcNumber}}{{/if}}
                {{else}}
                {{header.forwarder}}
                {{/if}}
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">5. CONSIGNEE TYPE</span>
            <div class="field-value">
                <span class="checkbox"></span> DIRECT CONSUMER<br>
                <span class="checkbox"></span> GOVERNMENT ENTITY<br>
                <span class="checkbox checked"></span> RESELLER<br>
                <span class="checkbox"></span> OTHER/UNKNOWN
            </div>
        </div>
    </div>

    <!-- Row 4: Ultimate Consignee -->
    <div class="row">
        <div class="field">
            <span class="field-label">6. ULTIMATE CONSIGNEE (Complete name and address)</span>
            <div class="field-value">
                {{#if header.consignee.name}}
                {{header.consignee.name}}<br>
                {{header.consignee.address}}{{#if header.consignee.address2}}, {{header.consignee.address2}}{{/if}}<br>
                {{header.consignee.city}}, {{header.consignee.state}} {{header.consignee.zip}}<br>
                {{header.consignee.country}}
                {{else}}
                {{header.consignee}}
                {{/if}}
            </div>
        </div>
    </div>

    <!-- Row 5: Reference Fields -->
    <div class="row">
        <div class="field">
            <span class="field-label">7. SHIPPER REF. #</span>
            <span class="field-value">
                {{#each references}}{{#if (eq type 'Shipment')}}{{value}}{{/if}}{{/each}}
            </span>
        </div>
        <div class="field">
            <span class="field-label">8. CONSIGNEE PO#</span>
            <span class="field-value">
                {{#each references}}{{#if (eq type 'PO')}}{{value}} {{/if}}{{/each}}
            </span>
        </div>
        <div class="field small-field">
            <span class="field-label">9. POINT (STATE) OF ORIGIN OR FTZ. NO.</span>
            <span class="field-value">{{#if header.shipper.state}}{{header.shipper.state}}{{/if}}</span>
        </div>
    </div>

    <!-- Row 6: Destination and Bond -->
    <div class="row">
        <div class="field">
            <span class="field-label">10. COUNTRY OF ULTIMATE DESTINATION</span>
            <span class="field-value">{{#if header.consignee.country}}{{header.consignee.country}}{{/if}}</span>
        </div>
        <div class="field small-field">
            <span class="field-label">11. IN BOND NUMBER</span>
            <span class="field-value"></span>
        </div>
        <div class="field small-field">
            <span class="field-label">12. ROUTED EXPORT TRANSACTION</span>
            <div class="field-value">
                <span class="checkbox"></span> YES
                <span class="checkbox checked"></span> NO
            </div>
        </div>
    </div>

    <!-- Row 7: Insurance and Declared Value -->
    <div class="row">
        <div class="field">
            <span class="field-label">13. INSURANCE</span>
            <span class="field-value"></span>
        </div>
        <div class="field">
            <span class="field-label">14. DECLARED VALUE FOR CARRIAGE $</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 8: Service Selection -->
    <div class="row">
        <div class="field">
            <span class="field-label">15. SHIPPER MUST CHECK INTERNATIONAL SERVICE REQUESTED:</span>
            <div class="field-value">
                <strong>AIR:</strong> <span class="checkbox"></span> NOW <span class="checkbox"></span> PREMIUM <span
                    class="checkbox"></span> VALUE <span class="checkbox"></span> DIRECT IATA<br>
                <strong>OCEAN:</strong> <span class="checkbox"></span> FCL <span class="checkbox"></span> LCL <span
                    class="checkbox"></span> PYRAMID LINES<br>
                <strong>GBL</strong>
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">16. TERMS OF FREIGHT PAYMENT AT LOCATION</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 9: Incoterms -->
    <div class="row">
        <div class="field">
            <span class="field-label">INCOTERMS® 2020 Rules for Any Mode or Modes of Transport:</span>
            <div class="field-value">
                <span class="checkbox {{#if (eq header.incoterm 'EXW')}}checked{{/if}}"></span> EXW (Ex Works)
                <span class="checkbox {{#if (eq header.incoterm 'FCA')}}checked{{/if}}"></span> FCA (Free Carrier)
                <span class="checkbox {{#if (eq header.incoterm 'CPT')}}checked{{/if}}"></span> CPT (Carriage Paid
                To)<br>
                <span class="checkbox {{#if (eq header.incoterm 'CIP')}}checked{{/if}}"></span> CIP (Carriage and
                Insurance Paid To)
                <span class="checkbox {{#if (eq header.incoterm 'DAP')}}checked{{/if}}"></span> DAP (Delivered at
                Place)<br>
                <span class="checkbox {{#if (eq header.incoterm 'DPU')}}checked{{/if}}"></span> DPU (Delivered at Place
                Unloaded)
                <span class="checkbox {{#if (eq header.incoterm 'DDP')}}checked{{/if}}"></span> DDP (Delivered Duty
                Paid)
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">INCOTERMS® 2020 Rules for Sea and Inland Waterway Transport:</span>
            <div class="field-value">
                <span class="checkbox {{#if (eq header.incoterm 'CFR')}}checked{{/if}}"></span> CFR<br>
                <span class="checkbox {{#if (eq header.incoterm 'CIF')}}checked{{/if}}"></span> CIF<br>
                <span class="checkbox {{#if (eq header.incoterm 'FAS')}}checked{{/if}}"></span> FAS<br>
                <span class="checkbox {{#if (eq header.incoterm 'FOB')}}checked{{/if}}"></span> FOB
            </div>
        </div>
    </div>

    <!-- Row 10: Special Instructions -->
    <div class="row">
        <div class="field">
            <span class="field-label">17. SPECIAL INSTRUCTIONS</span>
            <div class="field-value">
                These commodities, technology or software were exported from the United States in accordance with the
                Export Administration Regulations. Diversion contrary to U.S. law is prohibited.
            </div>
        </div>
    </div>

    <!-- Commodity Table -->
    <table>
        <thead>
            <tr>
                <th>18. D/F</th>
                <th>19. SCHEDULE B NUMBER (Description)</th>
                <th>20. QUANTITY SCHEDULE B UNIT(S)</th>
                <th>21. SHIPPING WEIGHT (kilograms)</th>
                <th>22. VALUE (U.S. dollar, omit cents)</th>
                <th>23. PIECES & DIMENSIONS (L x W x H in inches)</th>
            </tr>
        </thead>
        <tbody>
            {{#each lines}}
            <tr>
                <td>D</td>
                <td>{{htsCode}}<br>{{description}}<br>Part#: {{partNumber}}</td>
                <td class="text-right">{{quantity}}</td>
                <td class="text-right">{{netWeightKg}}</td>
                <td class="text-right">{{valueUsd}}</td>
                <td></td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <!-- Documents Section -->
    <div class="row" style="margin-top: 10px;">
        <div class="field">
            <span class="field-label">24. DOCUMENTS ATTACHED</span>
            <div class="field-value">
                <span class="checkbox checked"></span> COMMERCIAL INVOICE
                <span class="checkbox"></span> CERTIFICATE OF ORIGIN
                <span class="checkbox"></span> EXPORT LICENSE<br>
                <span class="checkbox"></span> LETTER OF CREDIT
                <span class="checkbox"></span> DG DECLARATION
                <span class="checkbox"></span> GBL
            </div>
        </div>
        <div class="field">
            <span class="field-label">25. TO BE PREPARED</span>
            <div class="field-value">
                <span class="checkbox"></span> PROFORMA INVOICE
                <span class="checkbox"></span> CERTIFICATE OF ORIGIN<br>
                <span class="checkbox"></span> EXPORT LICENSE
                <span class="checkbox"></span> INSURANCE CERTIFICATE
            </div>
        </div>
    </div>

    <!-- Authorization Section -->
    <div class="row">
        <div class="field">
            <span class="field-label">26. LICENSE NO./LICENSE EXCEPTION SYMBOL/AUTHORIZATION</span>
            <span class="field-value"></span>
        </div>
        <div class="field">
            <span class="field-label">28. DULY AUTHORIZED OFFICER OR EMPLOYEE</span>
            <div class="field-value" style="margin-top: 20px; border-top: 1px solid #000;">
                Signature Line
            </div>
        </div>
    </div>
</body>

</html>
