<!DOCTYPE html>
<html>

<head>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 9px;
            padding: 20px;
            line-height: 1.2;
        }

        .form-title {
            text-align: center;
            font-size: 14px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .row {
            display: flex;
            margin-bottom: 3px;
        }

        .field {
            border: 1px solid #000;
            padding: 3px 5px;
            flex: 1;
            position: relative;
        }

        .field-label {
            font-size: 7px;
            font-weight: bold;
            display: block;
            margin-bottom: 2px;
        }

        .field-value {
            font-size: 9px;
            min-height: 12px;
        }

        .small-field {
            flex: 0.4;
        }

        .medium-field {
            flex: 0.7;
        }

        .large-field {
            flex: 1.5;
        }

        .checkbox {
            display: inline-block;
            width: 10px;
            height: 10px;
            border: 1px solid #000;
            margin-right: 3px;
            vertical-align: middle;
        }

        .checkbox.checked::after {
            content: '✓';
            font-size: 10px;
            font-weight: bold;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 5px;
            font-size: 8px;
        }

        th,
        td {
            border: 1px solid #000;
            padding: 3px;
            text-align: left;
        }

        th {
            background: #f0f0f0;
            font-weight: bold;
            font-size: 7px;
        }

        .text-right {
            text-align: right;
        }

        .header-bar {
            background: #003087;
            color: white;
            padding: 5px;
            text-align: center;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .logo-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 5px;
        }

        .logo {
            font-size: 16px;
            font-weight: bold;
            color: #003087;
        }
    </style>
</head>

<body>
    <div class="logo-section">
        <div class="logo">
            {{#if header.forwarder.name}}
            {{header.forwarder.name}}
            {{else}}
            {{header.forwarder}}
            {{/if}}
        </div>
    </div>
    <div class="form-title">SHIPPER'S LETTER OF INSTRUCTION</div>

    <!-- Row 1: USPPI -->
    <div class="row">
        <div class="field large-field">
            <span class="field-label">1a. U.S. PRINCIPAL PARTY IN INTEREST (USPPI) (Complete name / address / contact
                name & tel.)</span>
            <div class="field-value">
                {{#if header.shipper.name}}
                {{header.shipper.name}}<br>
                {{header.shipper.address}}{{#if header.shipper.address2}}, {{header.shipper.address2}}{{/if}}<br>
                {{header.shipper.city}}, {{header.shipper.state}} {{header.shipper.zip}}<br>
                {{header.shipper.country}}
                {{else}}
                {{header.shipper}}
                {{/if}}
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">2. DATE OF EXPORTATION</span>
            <span class="field-value">{{date}}</span>
        </div>
        <div class="field small-field">
            <span class="field-label">3. TRANSPORTATION REFERENCE NO.</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 2: USPPI ID and Parties -->
    <div class="row">
        <div class="field">
            <span class="field-label">1b. USPPI (IRS) NO. or ID NO.</span>
            <span class="field-value"></span>
        </div>
        <div class="field">
            <span class="field-label">1c. PARTIES TO TRANSACTION CHECK APPLICABLE BOX</span>
            <div class="field-value">
                <span class="checkbox"></span> RELATED
                <span class="checkbox checked"></span> NON-RELATED
            </div>
        </div>
    </div>

    <!-- Row 3: Service Options -->
    <div class="row">
        <div class="field small-field">
            <span class="field-label">INSURANCE:</span>
            <div class="field-value">
                <span class="checkbox"></span> YES Insured Amount: _______<br>
                <span class="checkbox checked"></span> NO
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">FREIGHT CHARGES:</span>
            <div class="field-value">
                <span class="checkbox checked"></span> PREPAID<br>
                <span class="checkbox"></span> COLLECT
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">JETPAK SERVICE:</span>
            <div class="field-value">
                <span class="checkbox"></span> YES<br>
                <span class="checkbox checked"></span> NO
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">QUOTATION#:</span>
            <span class="field-value"></span>
        </div>
        <div class="field small-field">
            <span class="field-label">SERVICE LEVEL:</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 4: Ultimate Consignee -->
    <div class="row">
        <div class="field large-field">
            <span class="field-label">4a. ULTIMATE CONSIGNEE (Complete name / address and contact name & tel., if
                available)</span>
            <div class="field-value">
                {{#if header.consignee.name}}
                {{header.consignee.name}}<br>
                {{header.consignee.address}}{{#if header.consignee.address2}}, {{header.consignee.address2}}{{/if}}<br>
                {{header.consignee.city}}, {{header.consignee.state}} {{header.consignee.zip}}<br>
                {{header.consignee.country}}
                {{else}}
                {{header.consignee}}
                {{/if}}
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">TERM:</span>
            <div class="field-value">
                <span class="checkbox"></span> DOOR TO DOOR<br>
                <span class="checkbox"></span> PORT TO PORT<br>
                <span class="checkbox"></span> DOOR TO PORT<br>
                <span class="checkbox"></span> PORT TO DOOR<br>
                <span class="checkbox"></span> PORT TO CFS<br>
                <span class="checkbox"></span> CFS TO PORT<br>
                <span class="checkbox"></span> CFS TO CFS
            </div>
        </div>
    </div>

    <!-- Row 5: Consignee Type -->
    <div class="row">
        <div class="field">
            <span class="field-label">4b. ULTIMATE CONSIGNEE TYPE</span>
            <div class="field-value">
                <span class="checkbox"></span> DIRECT CONSUMER
                <span class="checkbox checked"></span> RESELLER
                <span class="checkbox"></span> GOVERNMENT ENTITY
                <span class="checkbox"></span> OTHERS/UNKNOWN: _______
            </div>
        </div>
    </div>

    <!-- Row 6: Intermediate Consignee -->
    <div class="row">
        <div class="field">
            <span class="field-label">4c. INTERMEDIATE CONSIGNEE (Complete name / address and contact name & tel., if
                available)</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 7: Incoterm -->
    <div class="row">
        <div class="field">
            <span class="field-label">INCOTERM:</span>
            <div class="field-value">
                <span class="checkbox {{#if (eq header.incoterm 'CIF')}}checked{{/if}}"></span> CIF
                <span class="checkbox {{#if (eq header.incoterm 'CIP')}}checked{{/if}}"></span> CIP
                <span class="checkbox {{#if (eq header.incoterm 'CFR')}}checked{{/if}}"></span> CFR
                <span class="checkbox {{#if (eq header.incoterm 'CPT')}}checked{{/if}}"></span> CPT
                <span class="checkbox {{#if (eq header.incoterm 'DAP')}}checked{{/if}}"></span> DAP
                <span class="checkbox {{#if (eq header.incoterm 'DDP')}}checked{{/if}}"></span> DDP<br>
                <span class="checkbox {{#if (eq header.incoterm 'DPU')}}checked{{/if}}"></span> DPU
                <span class="checkbox {{#if (eq header.incoterm 'EXW')}}checked{{/if}}"></span> EXW
                <span class="checkbox {{#if (eq header.incoterm 'FAS')}}checked{{/if}}"></span> FAS
                <span class="checkbox {{#if (eq header.incoterm 'FCP')}}checked{{/if}}"></span> FCP
                <span class="checkbox {{#if (eq header.incoterm 'FOB')}}checked{{/if}}"></span> FOB
                <span class="checkbox"></span> Other<br>
                NAMED PLACE/PORT: _______________________
            </div>
        </div>
    </div>

    <!-- Row 8: Forwarding Agent -->
    <div class="row">
        <div class="field large-field">
            <span class="field-label">5a. FORWARDING AGENT</span>
            <div class="field-value">
                {{#if header.forwarder.name}}
                <strong>{{header.forwarder.name}}</strong><br>
                {{header.forwarder.address}}{{#if header.forwarder.address2}}, {{header.forwarder.address2}}{{/if}}<br>
                {{header.forwarder.city}}, {{header.forwarder.state}} {{header.forwarder.zip}}<br>
                {{header.forwarder.country}}
                {{else}}
                {{header.forwarder}}
                {{/if}}
            </div>
        </div>
        <div class="field small-field">
            <span class="field-label">5b. FORWARDING AGENT'S EIN (IRS) NO.</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 9: Origin and Destination -->
    <div class="row">
        <div class="field">
            <span class="field-label">6. POINT OF ORIGIN OR FTZ NO.</span>
            <span class="field-value">{{#if header.shipper.state}}{{header.shipper.city}},
                {{header.shipper.state}}{{/if}}</span>
        </div>
        <div class="field">
            <span class="field-label">7. COUNTRY OF ULTIMATE DESTINATION</span>
            <span class="field-value">{{#if header.consignee.country}}{{header.consignee.country}}{{/if}}</span>
        </div>
        <div class="field small-field">
            <span class="field-label">8. LOADING PIER (Vessel only)</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 10: Mode and Carrier -->
    <div class="row">
        <div class="field">
            <span class="field-label">9. MODE OF TRANSPORTATION</span>
            <div class="field-value">
                <span class="checkbox"></span> AIR
                <span class="checkbox"></span> OCEAN
                <span class="checkbox"></span> TRUCK
                <span class="checkbox"></span> RAIL
            </div>
        </div>
        <div class="field">
            <span class="field-label">10. CARRIER IDENTIFICATION CODE</span>
            <span class="field-value"></span>
        </div>
        <div class="field">
            <span class="field-label">11. SHIPMENT REFERENCE NO.</span>
            <span class="field-value">
                {{#each references}}{{value}} {{/each}}
            </span>
        </div>
    </div>

    <!-- Row 11: Export Details -->
    <div class="row">
        <div class="field">
            <span class="field-label">12. EXPORTING CARRIER</span>
            <span class="field-value"></span>
        </div>
        <div class="field">
            <span class="field-label">13. PORT OF EXPORT</span>
            <span class="field-value"></span>
        </div>
        <div class="field small-field">
            <span class="field-label">14. ENTRY NUMBER</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 12: Bond, Hazmat, Port -->
    <div class="row">
        <div class="field">
            <span class="field-label">15. IN BOND NUMBER</span>
            <span class="field-value"></span>
        </div>
        <div class="field">
            <span class="field-label">16. HAZARDOUS MATERIALS</span>
            <div class="field-value">
                <span class="checkbox"></span> YES
                <span class="checkbox checked"></span> NO
            </div>
        </div>
        <div class="field">
            <span class="field-label">17. PORT OF UNLOADING (Vessel and Air Only)</span>
            <span class="field-value"></span>
        </div>
    </div>

    <!-- Row 13: Container and Case Mark -->
    <div class="row">
        <div class="field">
            <span class="field-label">18. CONTAINERIZED (Vessel Only)</span>
            <div class="field-value">
                <span class="checkbox"></span> YES
                <span class="checkbox"></span> NO
            </div>
        </div>
        <div class="field">
            <span class="field-label">19. CASE MARK (Required for Ocean JP Shpts)</span>
            <span class="field-value"></span>
        </div>
        <div class="field">
            <span class="field-label">20. ROUTED EXPORT TRANSACTION</span>
            <div class="field-value">
                <span class="checkbox"></span> YES
                <span class="checkbox checked"></span> NO
            </div>
        </div>
    </div>

    <!-- Commodity Table -->
    <div class="field">
        <span class="field-label">21. SCHEDULE B DESCRIPTION OF COMMODITIES (Use columns 22 to 32)</span>
    </div>

    <table>
        <thead>
            <tr>
                <th>22. D/F</th>
                <th>23. SCHEDULE B NUMBER / US HTS CODE (Must be 10-digits)</th>
                <th>24. QUANTITY</th>
                <th>25. SHIPPING WEIGHT (kg)</th>
                <th>26. VALUE (USD)</th>
                <th>27. VIN / PRODUCT NO. / VEHICLE TITLE NO.</th>
                <th>28. LICENSE NO. OR LICENSE EXCEPTION</th>
                <th>29. ECCN</th>
            </tr>
        </thead>
        <tbody>
            {{#each lines}}
            <tr>
                <td>D</td>
                <td>{{htsCode}}<br><small>{{description}}</small><br><small>Part#: {{partNumber}}</small></td>
                <td class="text-right">{{quantity}}</td>
                <td class="text-right">{{netWeightKg}}</td>
                <td class="text-right">{{valueUsd}}</td>
                <td>{{partNumber}}</td>
                <td></td>
                <td></td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <!-- Authorization Section -->
    <div class="row" style="margin-top: 10px;">
        <div class="field">
            <span class="field-label">I hereby certify that all information is true and correct, and all necessary
                documents are attached</span>
            <div class="field-value" style="margin-top: 30px; border-top: 1px solid #000;">
                Signature / Title / Date
            </div>
        </div>
    </div>
</body>

</html>
